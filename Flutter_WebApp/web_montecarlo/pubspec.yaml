name: web_montecarlo
description: A new Flutter project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  data_tables: ^1.3.2
  http: ^0.13.3
  indexed_list_view: ^2.0.0
  provider: ^5.0.0
  syncfusion_flutter_datagrid: ^19.1.59-beta

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
